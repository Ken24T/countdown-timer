# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
venv/
.venv/
ENV/
env/
virtualenv/
VENV/
# If you use other names for your venv, add them here

# IDE and editor specific files
.vscode/
.idea/
*.swp
*.swo
*~

# OS-specific files
.DS_Store
Thumbs.db

# Data files (if they are generated and shouldn't be versioned,
# otherwise, if timers_config.json should be versioned, remove data/timers_config.json)
# For now, assuming timers_config.json might be user-specific or too dynamic for versioning.
# If you want to version a template or default, consider a different approach.
# data/timers_config.json 

# Build artifacts
dist/
build/
*.egg-info/